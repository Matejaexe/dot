# Basic transparency (0.0 = fully transparent, 1.0 = fully opaque)
background_opacity 0.4

# Optional: Blur effect (works on Wayland with compositors like `sway` or `hyprland`)
# blur true
# blur_radius 10
